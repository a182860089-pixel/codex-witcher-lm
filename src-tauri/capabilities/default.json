{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal permissions for the local provider manager window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

